# 4elka — состояние работ

**Обновлено:** 24.08.2026. Написано для продолжения в новой сессии.

---

## Одним абзацем

**408 тестов зелёные, предупреждений ноль на чистой сборке, `make app` собирает
подписанный бандл** (внутри адаптер плеера и обе лицензии — своя MIT и чужая BSD-3).
Из 31 задачи плана сделано 28. Приложение ловит скриншоты и копирования, показывает
карточку из челки, держит историю с поиском и вкладками, полку файлов с AirDrop,
глобальный хоткей **⌃⌥V**, полное окно настроек и иконку в строке меню.

**Не готово к публикации:** остались сборка разделов панели (задача 30, в работе),
снимки для руководства и живые проверки, которые может сделать только человек.

---

## Что случилось 24.08: спасены двое умерших агентов

Лимит сессии убил трёх ревьюеров и оставил **двух исполнителей с несохранённой
работой**. Оба спасены и влиты:

**Оконная подсистема** (три критичных находки ревью, все про главный жест):
панель ложилась поверх зоны, ловящей нажатие, и съедала его — раскрытие по клику
было недостижимо; состояние наведения получало рамку ровно по вырезу, то есть по
области без пикселей, и не показывало ничего; карточка при раскрытой панели затирала
её содержимое навсегда. Представление панели стало чистым значением от пары
«состояние плюс очередь», применение к окнам — отдельный предъявитель. Плюс переезд
при смене экрана. Агент успел довести до зелёных тестов, но не успел закоммитить.

**Плеер:** жалобы адаптера уходили в `/dev/null`, и самый вероятный сбой — perl не
смог загрузить фреймворк — выходил наружу одной строкой «падает сразу», без пути и
без причины. Агент оставил красные тесты на недоделанный сборщик хвоста ошибок;
дописан по образцу соседнего буфера строк. Заодно относительный путь к адаптеру стал
отдельным отказом **до** запуска процесса.

**Урок:** агент, умерший от лимита, оставляет работу в воркри — её надо искать
(`git status` в каждом воркри), а не считать потерянной. Оба раза не хватало
маленького куска, дописанного за десять минут.

---

## Что осталось

| № | Задача | Состояние |
|---|---|---|
| 30 | Сборка разделов панели | **Сделана 24.08.** Плеер, история, полка, календарь, погода и заряды подключены; настройки протянуты до погоды, порогов заряда, календаря и плеера. Панель выросла с 640×340 до 712×672. Живьём не смотрели — нужно «го» |
| 32 | Настройки должны применяться | **Не начата.** Из шестнадцати настроек применялись четыре. Сочетание клавиш, три тумблера источников карточек, показ айфона — мёртвые ручки. Задача заведена, задание нарезано |
| 29 | Руководство со снимками | Текст написан целиком и сверён с кодом, помечен черновиком. Снимки ждут «го»: `scripts/capture-guide-shots.sh` снимает серию за один запуск. ⚠️ Снимок экрана **не показывает челку** |
| 28 | Порядок в git | README, changelog, `.gitattributes`, руководство готовы. Осталось: удалить воркри влитых агентов (список показан владельцу, ждёт слова), одна чистка истории от фикстур и тег версии |
| — | Починка полки и зарядов | **В работе:** два критичных дефекта потери файлов с полки плюс пять важных по зарядам |
Плюс в работе: починка потери данных в истории и починка плеера с жестом — по
находкам ревью.

---

## Что нашли ревью (четыре штуки, все закончены)

Ревьюили **по текущему состоянию `main`, а не по диффам**: историю писали двадцать с
лишним агентов, она переплетена, и код с тех пор уехал. Разбито по подсистемам.

### Четвёртое ревью, 24.08: погода, календарь, заряды, полка, хоткей, ядро

Эти подсистемы не смотрел никто. Нашлось **два критичных и двенадцать важных**, и
почти все — про молчание: код не различает «нет данных» и «не смог получить данные».

**Два критичных — оба про потерю данных в полке файлов.** Битый `shelf.json` молча
даёт пустую полку, и первая же запись затирает файл навсегда: рядом, в истории
буфера, ровно эта опасность описана в комментарии и решена — полка механизм не
использует. Второй: файл на отключённом томе (уснул NAS, вынули диск) неотличим от
удалённого, запись выметается и немедленно сохраняется — том вернулся, а записи нет.

**Заряды — четыре важных.** Строка `not charging` содержит слово `charging`, поэтому
остановленная оптимизированная зарядка читается как «на зарядке» и даёт карточку
«можно отключать». Ветка «заряжен, отключай» не смотрит на кабель вообще. Сорвавшийся
запуск утилиты стирает счётчики гистерезиса, и карточка выезжает второй раз на том же
заряде — **а тест закрепляет это как контракт**. У внешних утилит нет ни срока, ни
замка от наложения: заблокированный айфон вешает `ideviceinfo`, и за час это шестьдесят
намертво занятых потоков.

**Хоткей — три важных, и одно из них обиднее прочих: настройка сочетания ни на что не
влияет.** Выбор в окне настроек сохраняется на диск, а регистрируется всегда
сочетание по умолчанию. Плюс список предлагаемых сочетаний содержит системные —
`⌃Space` ломает переключение раскладки, `⌥Space` — неразрывный пробел. Занятое
сочетание сообщается только в системный журнал, то есть человек нажимает и не
понимает, почему тихо.

**Погода — молчание и тест-обманка.** Три разных исхода (нет сети, прокси отдал
заглушку, температура 900 градусов) дают одинаковый молчаливый выход. А тест
«запуск идемпотентен и не плодит таймеры» проверяет `calls <= 2` при реальном
значении 1 — то есть остаётся зелёным, даже если защиту из кода убрать.

**Что закрыто замерами сразу:** погода из его сети работает (HTTP 200 за полсекунды),
уровни заряда блютуса приходят строками и разбираются верно.

Полный отчёт с сценариями провала — в журнале.

### Ревью первых трёх подсистем (21–22.08)

Ревьюили **по текущему состоянию `main`, а не по диффам**: историю писали двадцать с
лишним агентов, она переплетена, и код с тех пор уехал. Разбито по подсистемам.

**Буфер обмена — четыре критичных.** Битый файл индекса молча стирает всю историю и
тут же перезаписывается пустым (проверено: 50 записей → 0, файл с 8480 байт до 216).
Файл картинки удаляется раньше, чем индекс перестал на него ссылаться, из-за чего
закреплённая картинка исчезает целиком. Нет сброса на диск при выходе: удалил из
истории пароль, вышел — он вернулся. `⌫` удаляет невидимое выделение с другой
вкладки. Плюс осиротевшие файлы не собираются никогда.

**Окна — три критичных**, см. выше. Задача 31.

**Плеер и жест — один критичный:** обработчик завершения старого процесса гасит уже
запущенный новый, и пять таких пар подряд выключают плеер до перезапуска
приложения. Дефект дремлет, потому что `stop()` не зовёт никто — сработает ровно в
момент подключения плеера к панели. Плюс: буфер строк не сбрасывается при
перезапуске потока (после обрыва панель уверенно показывает прошлый трек), одна
слишком длинная строка выбрасывает соседнюю здоровую, поток ошибок адаптера
отправлен в никуда — то есть самый вероятный сбой не диагностируется.

**Четвёртое ревью** (погода, календарь, заряды, действия над текстом, ядро, хоткей,
полка) — в работе. Эти подсистемы до сих пор не смотрел никто.

---

## Правила, выведенные болью

Это не стиль, а вещи, на которых уже спотыкались. Каждая стоила времени.

**Тесты запускать только через `./scripts/test.sh`.** В Command Line Tools нет модуля
`XCTest`, а `Testing.framework` есть, но SwiftPM не знает путей до него. Обвязка их
подставляет. Обход через `unsafeFlags` однажды дал **ложно-зелёный результат**:
сборка проходила, тестов запускалось ноль, код выхода был нулевой.

**Предупреждения мерить только так:** `rm -rf .build && ./scripts/test.sh 2>&1 | grep -c "warning:"`.
Обычный `swift build` тестовый таргет не видит. Повторный запуск без чистки тоже
врёт: перекомпилировать нечего.

**Тест, написанный по факту существующего поведения, закрепляет и дефект.** Ревью
нашло три таких: тест **без единой проверки** (убери защиту из кода — он останется
зелёным); тест, закрепляющий «битый файл = пустые данные» как контракт; тест,
проходящий по случайной причине и создающий ложную уверенность в живучести. Тест
должен выражать требование, а не наблюдение. Пример правильного: вместо «сочетание
равно ⌘⇧V» — «сочетание по умолчанию **не равно** системной вставке без
форматирования».

**Одно и то же значение, считаемое в двух местах, — источник трёх дефектов.**
Раскладка панели считалась в одном месте, размер окна в другом, и никто не сверял их
вместе. Днём это починили на карточке; ревью нашло то же самое в двух других
состояниях. Правильный ответ — чистая функция от состояния, накрытая тестом.

**Кто создаёт возможность — тот её и подключает.** Плеер, календарь, погода и заряды
оказались написаны, покрыты тестами и не подключены ни к чему, потому что каждая
задача считала подключение чужой работой, а задачи «собрать всё вместе» в плане не
было вовсе. Теперь правило записано в план, и в брифах оно требуется явно.

**Константы из `Config` в задание агенту не попадают** — генератор вырезает только
раздел своей задачи. Значение надо дублировать в текст задачи, иначе исполнитель
придумает своё, и план с кодом разойдутся молча. Попались трижды.

**Инструментовка, встроенная в проверяемую цепочку, сама становится дефектом.**
Первый замер плеера дал ноль карточек, и это почти попало в отчёт как «правильное
поведение» — а на самом деле пробник подменял обработчик и координатор не получал
ничего. Слушать надо то, что подсистема отдаёт наружу, ничего не подменяя.

**Снимок экрана не показывает физическую челку.** Система отдаёт кадр вместе с
областью за вырезом, поэтому чёрная фигура выглядит на снимке отдельным
прямоугольником в строке меню. «Стык незаметен» проверяется только человеком.

**Работа исполнителей — только в отдельных воркри.** Двое агентов в общем дереве
привели к тому, что несохранённые правки одного молча откатились.

**Гасить приложение только по имени исполняемого файла.** Шаблон
`pkill -f "4elka.*/Chelka"` совпадает и с процессами компилятора — уже убивал сборку
соседнего агента.

**Не запускать много агентов «на всякий случай»:** лимит сессии общий. Первый запуск
трёх ревьюеров умер целиком, не начав работу.

---

## Живые проверки: что закрыто и что нет

Полный список — `.superpowers/sdd/2026-08-21-4elka/needs-go.md`.

**Закрыто живьём 21.08:** карточка видна целиком (был дефект — вторая строка
обрезалась, потому что фигура была 54 точки при челке 38); фокус не крадётся (20
символов в редактор при пяти карточках — дошли все); заряды во всех состояниях,
включая исчезновение при отключении; мак на зарядке; плеер на ютубе без мигания
карточки; уборка брошенных процессов адаптера; бандл собирается и подписывается.

**Ждёт задачи 31:** наведение и клик по челке, карточка поверх раскрытой панели,
стекло на верхней полосе, смена монитора, строка меню на внешнем экране.

**Ждёт «го»:** сетка истории, перетаскивание в Finder, полка и AirDrop, окно
настроек, хоткей из другого приложения.

**Может сделать только владелец:** постоянный сертификат подписи (разовая подпись
означает, что после каждой пересборки система считает приложение новым и элемент
автозапуска ломается), первый пуш в GitHub, проверка менеджеров паролей.

---

## Как продолжать

1. **Журнал** — `.superpowers/sdd/2026-08-21-4elka/progress.md`. Вся история решений,
   включая отклонённые варианты и причины. Читать его, а не восстанавливать по коду.
2. **План** — `docs/superpowers/plans/2026-08-21-4elka.md`, 31 задача.
   **Спека** — `docs/superpowers/specs/2026-08-21-4elka-design.md`, там таблица
   замеров с живой машины: что проверено и чем. Замеры 21.08 отдельным блоком.
3. **Задание для агента** нарезается скриптом `task-brief`, но **раздел общих
   ограничений надо приклеивать к нему руками** — генератор его не берёт.
4. **После каждого мержа** сверять константы плана и кода: расхождение не ловится ни
   ревью диффа, ни отчётом исполнителя.
5. **Порядок остатка:** 31 (в работе) → 30 (сборка панели) → живая проверка по «го» →
   29 (руководство) → тег версии → пуш.

---

## Публикация

Репозиторий `github.com/adylenya/4elka` создан **публичным**, remote не добавлен,
пуш не делался ни разу. Лицензия **MIT**, раздача из исходников.

Готово: `README.md`, `CHANGELOG.md`, `.gitattributes`, `LICENSE`,
`THIRD-PARTY-NOTICES.md`, личные данные вычищены из истории, авторство коммитов
переписано на служебный адрес GitHub.

**Пуш делает владелец:**

```bash
git remote add origin https://github.com/adylenya/4elka.git
git push -u origin main --tags
```
